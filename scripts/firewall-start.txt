logger -t "FIREWALL_START" "Event: $1, Device: $2, Interface: $3"

[ "$(nvram get wans_dualwan)" = "usb none" ] || [ "$(nvram get wans_dualwan)" = "usb wan" ] && [ $(nvram get usb_modem_act_dev) = $3 ] && /jffs/scripts/fm350-firewall.sh
